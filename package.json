{
  "name": "pace-notes",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "find js tests -name '*.js' -print0 | xargs -0 -n1 node --check",
    "validate": "npm run check && npm test"
  }
}
